using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneManagerScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*GameObject radio = GameObject.Find("okean");
		AudioSource radioAduio = radio.GetComponent(typeof(AudioSource)) as AudioSource;
		radioAduio.Play();
		radioAduio.mute = true;*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
